# Case Library UI Tweaks

## 变更 1：删除按钮
案例详情页增加删除当前文章的功能，点击后确认删除，删除后回到案例列表。

## 变更 2：左侧视角栏固定
案例详情页滚动时，左侧的视角切换栏应该固定不动，只有右侧文章内容区滚动。

## 范围
- 仅修改案例详情页 `[product]/page.tsx`
- 新增一个 API 路由 `api/cases/delete` 处理删除
- 不超过 2 个文件
