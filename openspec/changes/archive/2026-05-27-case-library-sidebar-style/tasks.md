# Tasks — Sidebar Style Tweak

- [x] 1. 修改侧边栏按钮样式为方块卡片 + 图标文字组合
  - 将按钮改为 `rounded-2xl` 方块卡片风格
  - 每个视角项添加默认图标（material-symbols-outlined）
  - 调整 padding、gap、字体大小使视觉更舒适
  - 高亮态/默认态颜色保持现有方案

- [x] 2. 构建验证
  - 运行 `npx tsc --noEmit` 类型检查
  - 运行 `npx next build` 构建