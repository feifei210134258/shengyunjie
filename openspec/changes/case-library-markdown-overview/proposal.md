# 案例库 — 全局分析 + Markdown 渲染

## 动机

用户需要在案例库中看到产品的全局综合分析（而非仅特定视角），且 AI 生成内容需支持 Markdown 格式渲染。

## 目标

1. 新增"全局分析"视角 — 对产品进行综合分析，控制在 1000 字以内
2. AI 生成的文章内容支持 Markdown 格式显示（标题、列表、加粗等）

## 范围

- API: 新增 `overview` 视角，生成 prompt 调整为 ~1000 字综合分析
- 前端: 视角列表增加"全局分析"入口，引入 react-markdown 渲染文章内容